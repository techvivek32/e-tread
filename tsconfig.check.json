{
  "_comment": "Standalone strict typecheck for the two frontend files, so they can be verified before the real repo is available. Run: npm i -D typescript @types/react && npx tsc -p tsconfig.check.json",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "types": ["react"]
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "src/types/env.d.ts"]
}
