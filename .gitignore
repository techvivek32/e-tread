node_modules/
*.env
.env
.env.*
!.env.example
server/*.tmp
package-lock.json

# Runtime state: the encrypted broker token and the live bracket ledger.
# Wildcarded on purpose — .state, .state-prod, .state-anything. A token file reaching a
# remote is the one mistake in this repo that cannot be walked back.
.state*/
**/.state*/
